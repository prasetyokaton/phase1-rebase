version: '3'
services:
  dev:
    build: .
    container_name: phase1-dev
    ports:
      - "8501:8501"
    env_file: .env.dev
    volumes:
      - .:/app
    restart: always
    

  prod:
    build: .
    container_name: phase1-prod
    ports:
      - "8502:8501"
    env_file: .env.prod
    restart: always
    
